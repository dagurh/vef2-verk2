<%- include('header') -%>

<ul>
  <% if (categories) { %>
    <% for (const category of categories) { %>
      <li><a href="/spurningar/<%= category.name %>"><%= category.name %></a></li>
    <% } %>
  <% } else { %>
    <li>No categories available</li>
  <% } %>
  <li><a href="/form">Búa til flokk</a></li>
</ul>

<%- include('footer') -%>